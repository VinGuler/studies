/**
 * You have a functions that gets you all the URLs in an HTML page
 * And a function that receives a URL, and returns the HTML (or 404)
 * Without recursion (and with a legitimate complexity time), return all the URLs related to a given HTML page
 */