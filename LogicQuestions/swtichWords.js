/**
 * Implement a function that accepts a string ("BigWasp")
 * And an array with the words length in the string ([3, 4]).
 * The function will return a string with the words swapped.
 * Try to do it without any other array ("in place")
 * In the most efficient way
*/ 